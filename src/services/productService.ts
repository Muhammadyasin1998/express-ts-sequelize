// import Product from "../models/productModel";

// export const createProduct = async (userId: number, name: string, price: number, description: string) => {
//   return await Product.create({ userId, name, price, description });
// };

// export const getUserWithProduct = async (userId: number) => {
//   return await Product.findOne({ where: { userId }, include: ["user"] });
// };
